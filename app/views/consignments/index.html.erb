<center>
  <div class="client_content">
    <%=react_component('Consignment',{
      currentUserRole: current_user.role.role_name, 
      consignmentsJSON: @consignments.to_json(include: %i[dispatcher manager waybill goods])
      }) %>
  </div>
</center>