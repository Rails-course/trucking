<% if user_signed_in? %>
  <nav class="nav-wrapper">
    <div class="nav">
      <div class="nav-left">
        <%= link_to "Users", users_path, class:"share-links" %>
        <%= link_to "Companies", companies_path, class:"share-links" %>
        <%= link_to "Warehouses", warehouses_path, class:"share-links" %>
        <%= link_to "Consignments", consignments_path, class:"share-links" %>
        <%= link_to "Waybills", waybills_path, class:"share-links" %>
        <%= link_to "Write-off acts", write_off_acts_path, class:"share-links" %>
      </div>
      <div class="nav-right">
          <h3 id="nav-bar-info">
            <%= current_user.first_name %>
            <%= current_user.second_name %>
            -
            <%= current_user.role.role_name %>
            for
            <%= current_user.company.name %>
          </h3>
        <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'log-out-btn'%>
      </div>
    </div>
  </nav>
<% end %>

