<center>
  <div class="wrapper">
    <%=react_component('Users',{
        companiesJSON: @companies.to_json(), 
        rolesJSON: @roles.to_json(), 
        usersJSON: @users.to_json(include: { role: { only: [:role_name] },
                                            company: { only: [:name] },
                                            address: { only: %i[town street building apartment] }})
    }) %>
  </div>
</center>